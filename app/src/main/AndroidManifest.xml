<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rileystech.gurpay">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".activity.SelectGroup"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".activity.Dashboard" />
        <activity android:name=".activity.EditUser" />
        <activity android:name=".activity.EditGroup" />
        <activity android:name=".activity.NewBill" />
        <activity android:name=".activity.BillView" android:windowSoftInputMode="adjustPan" />
        <activity android:name=".activity.BillList" />
        <activity android:name=".activity.Launch"
            android:launchMode="singleTask"
            android:theme="@style/NoUiAppTheme"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>